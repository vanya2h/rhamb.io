export const styles = (theme) => ({
  head: {
    extend: [
      theme.utils.flexbox.col,
      theme.utils.flexbox.jc_c,
      theme.utils.flexbox.ai_c,
    ],
  },
  image: {
    width: '100%',
    maxWidth: '500px',
  },
});
